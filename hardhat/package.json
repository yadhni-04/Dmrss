{
  "name": "dmrs-hardhat",
  "version": "1.0.0",
  "devDependencies": {
    "hardhat": "^2.16.0",
    "@nomiclabs/hardhat-ethers": "^2.2.0",
    "ethers": "^5.7.0"
  },
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run --network localhost scripts/deploy.js"
  }
}
